#include "QueueModule.h"


/*
void createSortedOrder(){
    
    for (int i = 0; i < sizeof(m_UpOrders)-1; i++){
        if(currentFloor < i && m_UpOrders[i] == 1)
        {
            
        }
    }



}
*/


/*
Order getNextOrder(MotorDirection previousDir, int previousFloor) {
    
    if (motorDir == DIRN_UP){
        for(int i = previous_floor+1; i < 4; i++){
            if (m_UpOrders[i] == 1){
                Order ordre = {i, DIRN_UP};
                return ordre;
            }
        }
    }

    else if (motorDir == DIRN_DOWN){
        for(int i = previous_floor-1; i >= 0; i--){
            if (m_DownOrders[i] == 1){
                Order ordre = {i, DIRN_DOWN};
                return ordre;
            }
        }

    }

    Order emptyOrder = {-6, DIRN_STOP};
    return emptyOrder;

}

int sortOrder(MotorDirection previousDir){
        if (order_direction == stop)  
                order_direction = previousDir; // hvilken vei gikk motoren forrige gang
                if (order_direction == up){

                    if (m_nextdestinationup[] = 0 / tom;)

                         order_direction == down

                         if (m_nextdestinationdown[] = 0 / tom;)
                  
                if (order_dircetion == down)

                  if (m_nextdestinationdown[] = 0 / tom;)

                         order_direction == up

                         if (m_nextdestinationup[] = 0 / tom;)

                              // indiksasjon om at det e ingen ordre. skal stå i ro
        }
        if (order_direction== up){
            for (int i=0; i < 4; i++ ){
                if (current_floor)
            }
        }

}
*/



